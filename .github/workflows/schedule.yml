name: VM business-hours schedule

# The Europe/Berlin timezone keeps the schedule correct across CET and CEST.
# Start shortly before 09:00 so the platform is ready for business hours,
# then deallocate just after 15:00. Weekends have a stop-only safety check.
on:
  schedule:
    - cron: '55 8 * * 1-5'
      timezone: Europe/Berlin
    - cron: '5 15 * * 1-5'
      timezone: Europe/Berlin
    - cron: '15 8 * * 6,0'
      timezone: Europe/Berlin
  workflow_dispatch:
    inputs:
      action:
        description: 'Power action'
        required: true
        default: 'stop'
        type: choice
        options: [start, stop]

permissions:
  contents: read

concurrency:
  group: darsyria-vm-power
  cancel-in-progress: false

env:
  AZURE_RESOURCE_GROUP: DARSYRIA-RG
  AZURE_VM_NAME: darsyria-vm

jobs:
  power:
    runs-on: ubuntu-latest
    steps:
      - name: Decide the required power state
        id: decide
        env:
          MANUAL_ACTION: ${{ inputs.action }}
          TRIGGER_SCHEDULE: ${{ github.event.schedule }}
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            action="$MANUAL_ACTION"
          elif [[ "$TRIGGER_SCHEDULE" == "55 8 * * 1-5" ]]; then
            action=start
          else
            action=stop
          fi

          echo "Berlin time: $(TZ=Europe/Berlin date --iso-8601=seconds)"
          echo "action=$action" | tee -a "$GITHUB_OUTPUT"

      - name: Azure login
        if: steps.decide.outputs.action != 'none'
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Reconcile VM power state
        if: steps.decide.outputs.action != 'none'
        shell: bash
        run: |
          state=$(az vm get-instance-view \
            --resource-group "$AZURE_RESOURCE_GROUP" \
            --name "$AZURE_VM_NAME" \
            --query "instanceView.statuses[?starts_with(code, 'PowerState/')].code | [0]" \
            --output tsv)

          if [[ "${{ steps.decide.outputs.action }}" == "start" ]]; then
            if [[ "$state" == "PowerState/running" ]]; then
              echo "VM is already running."
            else
              az vm start \
                --resource-group "$AZURE_RESOURCE_GROUP" \
                --name "$AZURE_VM_NAME"
            fi
          elif [[ "$state" == "PowerState/deallocated" ]]; then
            echo "VM is already deallocated."
          else
            az vm deallocate \
              --resource-group "$AZURE_RESOURCE_GROUP" \
              --name "$AZURE_VM_NAME"
          fi
